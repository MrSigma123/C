/* Write function prototypes (in ANSI style) according to the
  function descriptions given below (omit the function definition)
  
  a) function package() displays the number of zeroes specified by
    passed argument of the type int
  b) function run() which takes two arguments of the int type and
    returns int type value
  c) function guess() which doesn't take any arguments and returns
    the int type value
  d) function floor() which takes the variable of the type double
    and that variable address, then the function writes passed
    variable under given address
*/

void package(int variable);
int run(int val1, int val2);
int guess(void);
void floor_function(double value, double * address);


