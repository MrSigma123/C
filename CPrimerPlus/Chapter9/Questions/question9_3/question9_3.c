/* Write function prorotypes (in ANSI style) according to the following
  function descriptions (omit the function definition):

  a) function n_char() which takes one int type argument and returns
    one char type argument
  b) function digits() which takes one double type argument and one
    int type argument and returns one int type value
  c) function which() which takes two addresses of the double type
    and returns one double type address
  d) function random() which doesn't take any arguments and returns
    one int type value */

char n_char(int character);
int digits(double digit);
double * which(double * address1, double * address2);
int random(void);
