/* simple program to get used to using variaables */
#include <stdio.h>
int main(void){
  int number;
  number = 10;
  printf("value = %d, value * 2 = %d, value2 = %d\n",
         number, number*2, number*number);
  return 0;
}
