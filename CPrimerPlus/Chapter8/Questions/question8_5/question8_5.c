/* What is EOF? */
#include <stdio.h>
int main(void) {
  printf("EOF is the macro which stores the system representation of \"End of line\"\n"
         "charcter which indicates that there is no next character stored in the file.\n");
  printf("Functions line scanf() and getchar() can use this value to stop reading the dara stream.\n");
  return 0;
}
