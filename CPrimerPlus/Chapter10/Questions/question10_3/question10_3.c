// What's the addres ref in question10_1 ? And ref + 1? What it points to?
#include <stdio.h>
int main(void) {
  printf("The ref address is the address of the first element of ref[] array.\n");
  printf("The address of an array + any decimal number reffers to specific array\n");
  printf("element. This notation is called \"pointer arithmetic.\"\n");
  return 0;
}
